package com.example.config;

import java.util.ArrayList;

//import info.jerrinot.subzero.Serializer;

public class ArrayListSerializer {
	/*
	 * extends Serializer<ArrayList>{
	 * 
	 * ArrayListSerializer() { super(ArrayList.class);
	 * 
	 * }
	 *//**
		 * TypeId has to be a unique for each registered serializer.
		 *
		 * @return TypeId of the class serialized by this serializer
		 *//*
			 * @Override public int getTypeId() { return 10000; }
			 */

}
